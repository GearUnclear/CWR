// Human tests and staged demonstrations run the primary campaign data too.
// Keep this as an include: a copied descriptor can silently disable systems
// (the old Showcase configuration omitted CIV and its population managers).
#include "Missions/Guerrilla.Abel/description.ext"
