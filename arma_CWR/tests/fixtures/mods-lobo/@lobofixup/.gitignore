# Patched THIRD-PARTY pbos (APL-SA @LoBo mod content) must never enter this
# GPL repo. They are regenerated byte-identically from the local @LoBo
# install - run gen-patched-pbos.ps1 once before first use of the lobo
# integration tests. The generator script and bin/config.cpp stay tracked.
*.pbo
