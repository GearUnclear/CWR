tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# Longest chain of the three civ tests: baseline economy tick (<= 20s after
# the shrink), forced scene (~30s with the walk collapsed), 50s panic
# episode with an economy tick inside it, then recovery + repopulation.
timeout = 360
assert_timeout = 90
extra_args = ["--dev"]
