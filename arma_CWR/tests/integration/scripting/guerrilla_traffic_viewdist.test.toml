tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# The fence watches the forced car across ~3 traffic passes (12 s sim) at
# view distance 5000; terrain paging at that range slows the frames a little.
timeout = 300
assert_timeout = 90
extra_args = ["--dev"]
