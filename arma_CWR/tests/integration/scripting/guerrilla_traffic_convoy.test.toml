tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# Traffic tick 5 s; force-spawn is immediate, the drive-off resolves within a
# tick or two, then a 30 s column watch and one or two ticks for the escort
# release after the quiet loss.
timeout = 300
assert_timeout = 120
extra_args = ["--dev"]
