tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# Traffic tick 5 s (+ 0.5 s commandeer sub-tick); force-spawn is immediate,
# the driving/despawn phases resolve within a tick or two.
timeout = 240
assert_timeout = 90
extra_args = ["--dev"]
