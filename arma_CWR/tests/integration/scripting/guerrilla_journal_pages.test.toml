tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# Boot + manager boot writes, then the real map display is opened and the
# dossier pages walked (Contents, Dispatches, Operations + its four pages,
# People + roster, Places + a zone page, Chronicles + record, Reference +
# chapters, the legacy aliases, link routing, live repaint); give the map
# construction + repaints room. This is the content/navigation lane; the two
# resolution-pinned capture lanes live in tests/integration/ui/
# guerrilla_journal_capture_{800,1440}.test.
timeout = 240
assert_timeout = 90
extra_args = ["--dev"]
