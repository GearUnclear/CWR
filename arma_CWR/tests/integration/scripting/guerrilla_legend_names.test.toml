tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# Boot, the seeding tick, the first companion spawn (the 5 s companion loop),
# then two award crossings each waited out against the registry's 1 Hz poll
# plus a few hundred sim frames of "nothing rerolls". Give it room.
timeout = 300
assert_timeout = 120
extra_args = ["--dev"]
