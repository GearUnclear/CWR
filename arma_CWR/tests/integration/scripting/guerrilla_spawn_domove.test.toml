tags = ["scripting", "guerrilla", "gate-zero", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_capture.Demo"
# Generous overall + assertion windows: the displacement assertion is polled
# in real time (framerate-agnostic), so foot AI needs seconds, not frames.
timeout = 240
assert_timeout = 90
extra_args = ["--dev"]
