tags = ["scripting", "guerrilla", "full_cwa", "headless"]
binary = "PoseidonGame"
mission = "tests/integration/missions/guerrilla_native.abel"
# Three commander spawns, a 45 s fire window, the tank branch's two waits and a
# 60 s negative window, all of them clock predicates against a 1 Hz poll. The
# assert timeout has to clear the longest single wait with room to spare.
timeout = 600
assert_timeout = 240
extra_args = ["--dev"]
